# CleanFeedVK — состояние и дорожная карта

Архив завершённых задач: **PROJECT_STATUS_ARCHIVE.md** (не перечитывать в каждой сессии).

**Правило:** Всё, что сделано и получило апрув (работает), переносить в PROJECT_STATUS_ARCHIVE. В PROJECT_STATUS оставлять только актуальные задачи и планы.

---

## Вводные по работе с проектом

- **Не запускать сборку/тесты из терминала** (xcodebuild, swift build и т.п.). Сборку и проверку делает пользователь в Xcode; в диалогах не пытаться «протестировать» через команды — это не требуется и не используется.
- По необходимости подтягивать контекст из **PROJECT_STATUS.md**, **TECHNICAL_NOTES.md**, **MESSAGES_SPEC.md**.

---

## 1. Что уже есть (кратко)

- **Лента:** посты с фото, видео, ссылками, опросами; лайки, комментарии, fullscreen фото/видео; тап по автору → группа/профиль; меню «три точки» у своих постов (Удалить), удаление фото в fullscreen для своих фото.
- **Профиль:** header (users.get), вкладки Стена / Фото / Друзья / Группы; альбомы, «Сохранённые фото», стена группы; на своей стене — меню поста «Удалить».
- **Таббар:** Лента | Друзья | Сообщения (заглушка) | Профиль.
- Детали по завершённым фичам — в PROJECT_STATUS_ARCHIVE.md.

---

## 2. Дорожная карта

**Осталось по плану:**
- **Сообщения** — (messages.get, messages.getConversations).

**Улучшения ленты (открытые):**
- **«Добавить в сохранённые»** — передача токена через `initialAccessToken` при открытии галереи (в fullScreenCover getAccessToken при тапе часто пуст).
- **Фото профиля в шапке** — при открытии аватара показывается миниатюра; нужно открывать фото в качестве «главной страницы» (из альбома «Фото профиля») или убрать растянутую миниатюру.

---

## 3. Блокеры и TODO (по приоритету)

**Бэклог (низкий приоритет):**
- Превью фото в сетке не подгружаются (placeholder в ленте/альбомах; fullscreen ок).

---

## 4. План: операции над постами, редактирование, сообщения

**Технические заметки** вынесены в **TECHNICAL_NOTES.md** (лента, видео, опросы, фото, OAuth). Туда же дописывать новые реализации.

**Сделано:** удаление поста (wall.delete), меню «три точки» с «Удалить» в ленте и на стене профиля; удаление фото (photos.delete) в fullscreen для своих фото; «Добавить в сохранённые» только для чужих фото. Детали — в PROJECT_STATUS_ARCHIVE.md.

**Приоритет (осталось):**

1. **Редактирование поста** — wall.edit(owner_id, post_id, message). Открывать экран/алерт с полем текста, сохранять через API, обновлять ячейку или ленту.
2. **Редактирование фото** — в fullscreen галерее уже есть «три точки» (showActionsOverlay). Добавить пункт «Редактировать подпись»: photos.edit(owner_id, photo_id, caption). Диалог ввода подписи, затем API и обновление.
3. **Сообщения** — см. **MESSAGES_SPEC.md**. Реализацию делать после редактирования поста/фото.
