# CleanFeedVK — состояние и дорожная карта

Архив завершённых задач: **PROJECT_STATUS_ARCHIVE.md** (не перечитывать в каждой сессии).

**Правило:** Всё, что сделано и получило апрув (работает), переносить в PROJECT_STATUS_ARCHIVE. В PROJECT_STATUS оставлять только актуальные задачи и планы.

---

## Вводные по работе с проектом

- **Не запускать сборку/тесты из терминала** (xcodebuild, swift build и т.п.). Сборку и проверку делает пользователь в Xcode; в диалогах не пытаться «протестировать» через команды — это не требуется и не используется. В Xcode работа в той же папке - изменения подтягиваются автоматически.
- По необходимости подтягивать контекст из **PROJECT_STATUS.md**, **TECHNICAL_NOTES.md**, **MESSAGES_SPEC.md**.

---

## 1. Что уже есть (кратко)

- **Лента:** посты с фото, видео, ссылками, опросами; лайки, комментарии, репостами fullscreen фото/видео; тап по автору → группа/профиль; меню «три точки» у своих постов (Удалить), удаление фото в fullscreen для своих фото.
- **Профиль:** header (users.get), вкладки Стена / Фото / Друзья / Группы; альбомы, «Сохранённые фото», стена группы, профиля; функции удалить пост / фото.
- **Таббар:** Лента | Друзья | Сообщения (заглушка) | Профиль.
- Детали по завершённым фичам — в PROJECT_STATUS_ARCHIVE.md.

---

## 2. Дорожная карта

**Осталось по плану:**
- **Сообщения** — (messages.get, messages.getConversations).

**Улучшения ленты (открытые):**
- **«Добавить в сохранённые»** — не удается добавить картинку в "Сохраненные"
- **Фото профиля в шапке** — при открытии аватара показывается миниатюра; нужно открывать фото в качестве «главной страницы» (из альбома «Фото профиля») или убрать растянутую миниатюру.

---

## 3. Блокеры и TODO (по приоритету)

**Текущие проблемы (требуют доработки):**
- **«Сделать фото профиля» — пустой токен в fullScreenCover:** API исправлен (photos.makeCover + album_id -6), тосты успех/ошибка и текст ошибки VK добавлены. При нажатии в галерее приходит пустой токен («Нет токена доступа» / «Войдите в аккаунт снова»). Нужно разобрать передачу/захват токена при показе fullScreenCover (getAccessToken, authService, capturedTokenForSave).

**Бэклог (низкий приоритет):**
- Превью фото в сетке не подгружаются (placeholder в ленте/альбомах; fullscreen ок).

---

## 4. План: операции над постами, редактирование, сообщения

**Технические заметки** вынесены в **TECHNICAL_NOTES.md** (лента, видео, опросы, фото, OAuth). Туда же дописывать новые реализации.

**Сделано в предыдущих сессиях:** удаление поста/фото, меню поста, «Добавить в сохранённые» только для чужих фото; отступы стены (LazyVStack spacing + padding, регулируемо); превью фото — фикс. высота/миниатюра; photos.makeCover (album_id, тосты, возврат текста ошибки). Детали — в PROJECT_STATUS_ARCHIVE.md.

**Приоритет (осталось):**

1. **Токен в fullScreenCover** — исправить пустой токен при «Сделать фото профиля» (см. п. 3).
2. **Закрепить пост** — в «три точки» своих постов добавить wall.pin.
3. **Дата поста** — показывать дату публикации в постах на стене.
4. **Редактирование поста** — wall.edit(owner_id, post_id, message). Экран/алерт с полем текста, API, обновление ячейки.
5. **Редактирование подписи фото** — в fullscreen «три точки»: photos.edit(caption), диалог ввода.
6. **Сообщения** — см. **MESSAGES_SPEC.md**.
